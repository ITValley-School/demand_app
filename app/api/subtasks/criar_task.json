{
  "meta": {
    "$_POST": [
      {
        "type": "number",
        "name": "entregavel_id"
      },
      {
        "type": "text",
        "name": "nome"
      },
      {
        "type": "text",
        "name": "descricao"
      },
      {
        "type": "number",
        "name": "estimativa_horas"
      }
    ]
  },
  "exec": {
    "steps": {
      "name": "inserir_task",
      "module": "dbupdater",
      "action": "insert",
      "options": {
        "connection": "dbmasterclasse",
        "sql": {
          "type": "insert",
          "values": [
            {
              "table": "itv.tasks",
              "column": "entregavel_id",
              "type": "number",
              "value": "{{$_POST.entregavel_id}}"
            },
            {
              "table": "itv.tasks",
              "column": "nome",
              "type": "text",
              "value": "{{$_POST.nome}}"
            },
            {
              "table": "itv.tasks",
              "column": "descricao",
              "type": "text",
              "value": "{{$_POST.descricao}}"
            },
            {
              "table": "itv.tasks",
              "column": "estimativa_horas",
              "type": "number",
              "value": "{{$_POST.estimativa_horas}}"
            },
            {
              "table": "itv.tasks",
              "column": "status",
              "type": "text",
              "value": "Pendente"
            }
          ],
          "table": "itv.tasks",
          "returning": "id",
          "query": "select top(0) [t].[id] into #out from [itv].[tasks] as t left join [itv].[tasks] on 0=1;insert into [itv].[tasks] ([descricao], [entregavel_id], [estimativa_horas], [nome], [status]) output inserted.[id] into #out values (@p0, @p1, @p2, @p3, @p4); select [id] from #out; drop table #out;",
          "params": [
            {
              "name": ":P1",
              "type": "expression",
              "value": "{{$_POST.entregavel_id}}",
              "test": ""
            },
            {
              "name": ":P2",
              "type": "expression",
              "value": "{{$_POST.nome}}",
              "test": ""
            },
            {
              "name": ":P3",
              "type": "expression",
              "value": "{{$_POST.descricao}}",
              "test": ""
            },
            {
              "name": ":P4",
              "type": "expression",
              "value": "{{$_POST.estimativa_horas}}",
              "test": ""
            }
          ]
        }
      },
      "meta": [
        {
          "name": "identity",
          "type": "text"
        },
        {
          "name": "affected",
          "type": "number"
        }
      ]
    }
  }
}