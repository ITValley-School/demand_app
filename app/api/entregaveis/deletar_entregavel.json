{
  "meta": {
    "$_GET": [
      {
        "type": "number",
        "name": "id"
      }
    ]
  },
  "exec": {
    "steps": {
      "name": "delete_entregavel",
      "module": "dbupdater",
      "action": "delete",
      "options": {
        "connection": "dbmasterclasse",
        "sql": {
          "type": "delete",
          "table": "itv.entregaveis",
          "wheres": {
            "condition": "AND",
            "rules": [
              {
                "id": "id",
                "field": "id",
                "type": "double",
                "operator": "equal",
                "value": "{{$_GET.id}}",
                "data": {
                  "column": "id"
                },
                "operation": "="
              }
            ],
            "conditional": null,
            "valid": true
          },
          "returning": "id",
          "query": "select top(0) [t].[id] into #out from [itv].[entregaveis] as t left join [itv].[entregaveis] on 0=1;delete from [itv].[entregaveis] output deleted.[id] into #out where [id] = @p0; select [id] from #out; drop table #out;",
          "params": [
            {
              "operator": "equal",
              "type": "expression",
              "name": ":P1",
              "value": "{{$_GET.id}}",
              "test": ""
            }
          ]
        }
      },
      "meta": [
        {
          "name": "affected",
          "type": "number"
        }
      ]
    }
  }
}